using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class OverlayHudManager : MonoBehaviour {

    public enum HudImages { PhiChart, ObjectiveLine, ActiveWeaponEmpty, ActiveWeaponFull, AltWeaponLeft, AltWeaponRight, WeaponArrowleft, WeaponArrowRight, LevelIcon, LevelBarEmpty, LevelBarFull, PowerupIndicator, HintSpeechbubble};
    public enum HudTexts { ObjectiveTitle, ObjectiveStatus, LevelCount, ExpPercentage, HintText };

	public void ActivateHud() {

    }
}
